# Đề xuất FE — 4 việc, giao cho Codex

Ngày: **2026-08-09** · Nhánh: `giap` (đã merge `origin/main` tại `405a8d0`)
Trạng thái verify lúc soạn: `flutter analyze --no-fatal-infos` 0 error / 0 warning · `flutter test` 304/304 pass

Bốn việc độc lập nhau, làm riêng từng cái được. **Việc 1 là bug thật đang chặn tính năng, nên làm trước.**

---

## Việc 1 — Bản đồ không hiện với SOS phát từ đồng hồ · BUG · ƯU TIÊN CAO NHẤT

### Nguyên nhân: sai tên field, im lặng

Swagger `SosAlertResponseDto` khai mảng điểm vị trí tên là **`locationPoints`**:

```
python -c "import json;d=json.load(open('family-care-api.json',encoding='utf-8'));print([k for k in d['components']['schemas']['SosAlertResponseDto']['properties'] if 'ocation' in k])"
→ ['locationPoints']
```

Nhưng FE đọc bằng tên khác — [`lib/screens/shared/sos_screen.dart:1417`](lib/screens/shared/sos_screen.dart:1417):

```dart
({double lat, double lng})? _location(Map<String, dynamic> d) {
  for (final key in ['locations', 'sosLocations']) {   // ← KHÔNG có key nào tồn tại
    ...
  }
  final lat = _d(d['latitude'] ?? d['initialLatitude']);   // ← chỉ còn nhánh này chạy
  ...
}
```

Hệ quả:

- Cảnh báo từ **điện thoại** vẫn có bản đồ, vì `initialLatitude/Longitude` được gửi ngay lúc tạo.
- Cảnh báo từ **đồng hồ** không có bản đồ, vì BE là bên tạo alert nên `initialLatitude` = null, còn điểm vị trí FE đẩy lên sau thì **không bao giờ được đọc**.

Commit `fc8fcbe` đã làm đồng hồ đẩy điểm vị trí đúng contract (`WEARABLE_GPS` + `deviceId`), nhưng bên nhận không đọc được nên nhìn từ ngoài vẫn y như chưa sửa gì.

### Cần làm

1. Trong `_location()`, thêm **`locationPoints`** vào đầu danh sách key. Giữ `locations`/`sosLocations` làm dự phòng cho dữ liệu cũ.
2. Sắp xếp theo thời gian trước khi lấy điểm cuối. Hiện code lấy `last` của mảng và **giả định BE trả theo thứ tự tăng dần** — không có gì bảo đảm. Dùng `recordedAt` (có trong `SosLocationPointResponseDto`), thiếu thì lùi về `createdAt`.
3. Rà cùng kiểu lỗi ở các hàm đọc mảng lân cận trong file: `_responses()` đang đọc `['responses','sosResponses','alertResponses']` — Swagger khai `responses`, khớp, không cần sửa. Nhưng nên quét thêm cả file xem còn key nào FE tự đặt.

### Vấn đề đi kèm: sheet chi tiết không tải lại

[`sos_screen.dart:1360`](lib/screens/shared/sos_screen.dart:1360) gọi `fetchAlertDetail` **đúng một lần** khi mở. Điểm vị trí từ đồng hồ được đẩy lên ngay sau khi alert tạo (chênh 1–2 giây). Người nhà mở thông báo tức thì sẽ thấy sheet không có bản đồ và nó **không bao giờ tự cập nhật**.

Đề xuất: khi `alert.status == 'ACTIVE'` và `_location()` trả null, cho sheet thử lại `fetchAlertDetail` sau ~5 giây, tối đa 2–3 lần rồi thôi. Không poll vô hạn.

Có sẵn `SosProvider.fetchCurrentLocation(alertId)` (`GET .../location/current`) — BE làm riêng cho "người theo dõi vừa vào xem". Hiện chỉ dùng ở [`sos_screen.dart:788`](lib/screens/shared/sos_screen.dart:788). Dùng luôn hàm này trong sheet chi tiết là hợp lý hơn tự retry `fetchAlertDetail`.

### Kiểm chứng

Không có widget test nào phủ màn SOS. Nên **thêm test mapping thuần** cho `_location()` — tách nó thành hàm top-level private hoặc `@visibleForTesting`, rồi khoá 4 case: chỉ có `locationPoints`; chỉ có `initialLatitude`; có cả hai (ưu tiên điểm mới nhất); không có gì (trả null). Đây đúng loại bug mà test mapping bắt được, và repo đã có tiền lệ (`test/wearable_mapping_test.dart`).

Sau đó test tay: đồng hồ bắn cảm biến → tài khoản Hoh mở cảnh báo → phải thấy bản đồ.

---

## Việc 2 — Đồng hồ dùng GPS của chính nó, bỏ lùi về vị trí điện thoại

### Bối cảnh

Commit `fc8fcbe` thêm [`resolveWearableSosPosition()`](lib/services/sos_location.dart) với thứ tự: GPS đồng hồ → nếu null thì lùi về vị trí điện thoại cùng tài khoản (`GpsProvider.shares`). Lý do lúc đó: máy ảo Wear OS mặc định không có toạ độ nên demo sẽ trắng bản đồ.

Nay chốt lại: **chỉ dùng GPS của chính đồng hồ.** Vị trí người đeo mới là thứ có nghĩa trong tình huống khẩn cấp; lấy vị trí điện thoại là nói dối — điện thoại có thể đang ở nhà trong khi người đeo ngã ngoài đường.

### Cần làm

1. Bỏ tham số `fallbackLat`/`fallbackLng` khỏi `resolveWearableSosPosition()`, hoặc bỏ hẳn hàm này và gọi thẳng `resolveSosPosition()`. Cập nhật doc comment — hiện đang giải thích lý do lùi, sẽ thành sai.
2. [`wear_sensor_sos_screen.dart`](lib/wear/screens/wear_sensor_sos_screen.dart) `_pushPosition()`: bỏ đoạn đọc `GpsProvider.shares`, bỏ import `gps_provider` và `auth_provider` nếu không còn dùng chỗ khác.
3. [`wear_sos_screen.dart`](lib/wear/screens/wear_sos_screen.dart): SOS thủ công hiện lấy `gps.shares` cho `initialLatitude/Longitude` lúc tạo cảnh báo. Đổi sang GPS đồng hồ, **nhưng giữ nguyên nguyên tắc không làm chậm nút khẩn cấp**: `resolveSosPosition()` có thể mất tới 10 giây, không được đặt trước lời gọi tạo SOS. Cách đúng là tạo SOS trước không kèm toạ độ, rồi đẩy điểm vị trí ngay sau — giống hệt luồng cảm biến.
4. Cập nhật `API_DOCS.md` mục `[BUG FE 2026-08-07]`: bỏ câu nói về bước lùi vị trí điện thoại.

### Rủi ro phải nói trước

Máy ảo Wear OS **không có toạ độ GPS mặc định**. Sau thay đổi này, muốn thấy bản đồ khi demo thì bắt buộc vào Extended controls → Location → đặt một điểm → Set. Nếu quên, bản đồ trống và rất dễ tưởng là code hỏng.

Nên thêm dòng trạng thái nhỏ trên màn SOS đồng hồ: khi `resolveSosPosition()` trả null thì hiện "Không lấy được vị trí" thay vì im lặng — để phân biệt "chưa có GPS" với "code hỏng".

---

## Việc 3 — Rà lỗi tràn layout trên toàn bộ màn đồng hồ

### Bối cảnh

Màn đếm ngược té ngã vẫn báo `BOTTOM OVERFLOWED BY 4.0 PIXELS` dù commit `fc8fcbe` đã cắt 10px (icon 24→20, các khoảng cách 5→4, 3→2, 8→6, 6→4).

Hai khả năng, cần phân biệt trước khi sửa tiếp:

- Ảnh chụp từ bản build **trước** khi vá → chạy lại `flutter run` và xác nhận.
- 10px vẫn chưa đủ trên thiết bị/độ phóng chữ đó.

Dù là khả năng nào, **cắt thêm vài pixel không phải cách sửa đúng**. Màn này bị bó chiều cao cứng, mà chiều cao thật phụ thuộc kích thước đồng hồ, cỡ chữ hệ thống người dùng đặt, và độ dài chuỗi tiếng Việt. Cắt đủ cho một máy không có nghĩa đủ cho máy khác.

### Cách sửa nên theo

Trong `WearPage` (hoặc ngay tại chỗ dùng), áp mẫu **"căn giữa khi vừa, cuộn khi không vừa"**:

```dart
LayoutBuilder(
  builder: (ctx, c) => SingleChildScrollView(
    child: ConstrainedBox(
      constraints: BoxConstraints(minHeight: c.maxHeight),
      child: Column(mainAxisAlignment: MainAxisAlignment.center, children: [...]),
    ),
  ),
)
```

Nội dung vừa màn thì vẫn căn giữa như hiện tại; dài hơn thì cuộn được thay vì tràn. Không mất nút nào.

Riêng màn đếm ngược té ngã có ràng buộc thêm: **cả "Con ổn" lẫn "Gửi SOS" phải nằm sẵn trong tầm mắt**, không được bắt cuộn mới thấy. Nên với màn này, ưu tiên giữ đủ chỗ bằng cách rút gọn nội dung (bỏ bớt một dòng phụ, hoặc gộp "Bạn có ổn không?" và dòng đếm ngược làm một) trước khi cho phép cuộn.

### Phạm vi rà

15 chỗ gọi `WearPage(` không có `scrollable` — đều là ứng viên tràn:

```
wear_alerts_screen.dart:63      wear_calendar_screen.dart:32
wear_home_screen.dart:61        wear_map_screen.dart:38
wear_notifications_screen.dart:36  wear_pairing_screen.dart:148
wear_quick_message_screen.dart:85  wear_sensor_sos_screen.dart:278, 365, 437
wear_sos_screen.dart:191, 275, 286  wear_status_screen.dart:37
wear_tasks_screen.dart:34
```

Mở lần lượt trên máy ảo Wear OS, ưu tiên các màn có nhiều nút hoặc chuỗi dài. Ghi lại màn nào tràn bao nhiêu px trước khi sửa, để biết sửa xong có hết thật không.

Nên thử thêm ở **cỡ chữ hệ thống lớn** (Settings → Display → Font size) — đây là nguồn tràn phổ biến nhất và chưa ai thử.

### Không đụng tới

Phía điện thoại: Duy đã sửa tràn ở card "Tổng quan hôm nay" (`1db67f9`). Nếu rà cả mobile thì làm đợt riêng, đừng trộn vào đợt này.

---

## Việc 4 — Đăng nhập Google khi email đã tồn tại trong gia đình

### Sự thật cần biết trước khi chọn phương án

BE đã trả lời chính thức ngày 2026-08-07 (xem `CAU_HOI_BE_2026-08-07.md` mục D1, D3):

> **D1.** Google login trùng email với account password cũ sẽ **link/gộp vào account cũ**, không tạo user mới.
> **D3.** Google login/create/link đều set `verificationStatus = VERIFIED`.

Và `POST /auth/firebase` trong Swagger chỉ khai `200 / 401 / 403 / 503` — **không có 409, không có cờ nào phân biệt "vừa tạo mới" với "vừa gộp vào tài khoản cũ"**. Cả hai trường hợp đều trả `200` kèm token hợp lệ, hình dạng response giống hệt nhau.

### Đánh giá 3 hướng anh nêu

**TH1 — cho đăng nhập thẳng vào tài khoản đó.**
Đây **đã là hành vi hiện tại của BE**, không phải lựa chọn FE có thể bật/tắt. Không có việc gì để làm.

**TH2 — cảnh báo "tài khoản đã tồn tại, hãy dùng email khác".**
**Không nên làm**, vì hai lý do:

- Về nghiệp vụ: đó là tài khoản **của chính người đang đăng nhập** — cùng email nghĩa là cùng người. Bắt họ dùng email khác là khoá họ khỏi tài khoản và dữ liệu gia đình của chính mình. Cách các hệ thống lớn làm (Google, GitHub) đều là gộp, không chặn.
- Về kỹ thuật: FE **không có cách nào biết** trường hợp nào đang xảy ra. Response hai trường hợp giống nhau hoàn toàn. Muốn cảnh báo thì phải đoán, mà đoán sai là chặn nhầm người dùng mới.

**TH3 — đề xuất của tôi: không chặn, mà nói rõ sau khi gộp.**

Vấn đề thật không phải "cho hay không cho", mà là **người dùng không biết chuyện gộp đã xảy ra**. Họ bấm "Tiếp tục với Google", vào thẳng một tài khoản có sẵn dữ liệu, và không ai nói gì cả. Lần sau quên mất mình từng có mật khẩu, đi bấm "Quên mật khẩu" thì lại rơi vào lỗi BE đã xác nhận: tài khoản Google-only nhận `200` nhưng **không có OTP nào được gửi**.

Việc cần làm chia hai phần:

**Phần cần BE (chặn phần hiển thị, không chặn đăng nhập):**

Xin BE thêm **một trường boolean** vào response `POST /auth/firebase`, ví dụ:

```json
{ "accessToken": "...", "refreshToken": "...", "user": {...}, "linkedExistingAccount": true }
```

`true` = email này đã có tài khoản từ trước và vừa được liên kết với Google. `false` = tài khoản mới tạo.

Đây là thứ **duy nhất** FE cần để làm đúng. Không cần endpoint mới, không cần đổi luồng.

**Phần FE làm được ngay, không cần chờ BE:**

1. **Màn đăng nhập** — thêm một dòng nhỏ dưới nút Google: *"Nếu email này đã có tài khoản, hệ thống sẽ liên kết vào tài khoản đó."* Đặt kỳ vọng đúng trước khi bấm, thay vì giải thích sau.
2. **Màn quên mật khẩu** — gợi ý cho tài khoản Google đã thêm ở commit trước, giữ nguyên.
3. **Màn Hồ sơ** — hiện phương thức đăng nhập của tài khoản đang dùng (mật khẩu / Google / cả hai). Người dùng tự biết mình đăng nhập kiểu gì thì đỡ hẳn nhóm lỗi này. Cần BE trả thông tin đó trong `/auth/me`, nên gộp chung vào lần xin BE ở trên.

**Sau khi BE có cờ:** hiện một thông báo một lần ngay sau khi đăng nhập Google thành công với `linkedExistingAccount = true`:

> Đã liên kết Google với tài khoản sẵn có của bạn (`email@…`). Từ giờ bạn đăng nhập bằng Google hoặc mật khẩu đều được.

Chỉ hiện một lần, không chặn luồng, không bắt xác nhận.

### Việc cần Codex làm ở lượt này

Chỉ phần FE không cần chờ BE (mục 1 và 3 ở trên — mục 2 đã xong), **cộng soạn nội dung xin BE** hai thứ: cờ `linkedExistingAccount` cho `/auth/firebase`, và phương thức đăng nhập trong `/auth/me`. Viết theo mẫu các file `DE_XUAT_BE_*.md` sẵn có: method, path, request/response, mức độ **Bắt buộc** vs **Nên có**.

**Không** implement TH2. Nếu thấy lý do phản đối chưa thuyết phục thì dừng lại hỏi, đừng tự làm.

---

## Thứ tự đề nghị

| # | Việc | Vì sao xếp ở đây |
|---|---|---|
| 1 | Bản đồ SOS từ đồng hồ | Bug thật, chặn tính năng, sửa gọn trong một hàm |
| 2 | GPS riêng của đồng hồ | Phụ thuộc việc 1 để kiểm chứng được (không có bản đồ thì không biết vị trí đúng chưa) |
| 3 | Rà tràn layout đồng hồ | Độc lập, nhưng cần chạy máy ảo nhiều lượt nên tốn thời gian |
| 4 | Đăng nhập Google | Phần lớn là chờ BE; phần FE nhỏ, làm lúc nào cũng được |

## Ràng buộc chung

- Quy ước repo: tài liệu, comment, commit message bằng **tiếng Việt**.
- Rule 1: đụng contract API thì cập nhật `API_DOCS.md` **trong cùng thay đổi**.
- Rule 2: BE thiếu gì thì dừng lại viết đề xuất, không mock/workaround.
- Rule 3: preview trước khi sửa — mô tả file, logic cũ → mới, rủi ro.
- Chạy `flutter analyze --no-fatal-infos` (phải 0 error) và `flutter test` (hiện 304/304) sau mỗi việc.
- `family-care-api.json` trong repo **đang lỗi thời** (226 path, thiếu `daily-brief`, `uiHints`, `ACTION_PLAN_CARD`, endpoint confirm theo `actionIndex`). Cần export lại từ Swagger trước khi dựa vào nó để đối chiếu.
