# Câu hỏi cho BE — đối chiếu contract FE/BE, ngày 2026-08-09

**Người hỏi:** FE Mobile (Flutter — app điện thoại + app Wear OS dùng chung codebase)
**Người nhận:** Nhật (lead) + Nghĩa (BE), hoặc AI đang đọc source BE
**Bản Swagger đang dùng:** **229 path / 296 operation / 253 schema**, lấy ngày 09/08/2026

---

## ⚠️ Đọc kỹ phần này trước

**Tài liệu này CHỈ LÀ CÂU HỎI. Không có dòng nào ở đây là yêu cầu BE sửa code hay thêm endpoint.**

Cụ thể là:

- **Không** yêu cầu thêm endpoint mới nào.
- **Không** yêu cầu đổi tên field, đổi enum, hay đổi schema nào.
- **Không** yêu cầu deadline.
- FE **cũng sẽ không tự sửa code** theo suy đoán của mình trước khi có câu trả lời — đây là lý do
  chính bọn mình dừng lại hỏi thay vì cứ làm tiếp.

**Vì sao phải hỏi trước khi làm bất cứ gì:**

FE hiện đang đọc khá nhiều field mà Swagger không mô tả (chi tiết trong từng phần bên dưới). Mỗi
chỗ như vậy đều có đúng ba khả năng, và **ba khả năng này dẫn tới ba hành động hoàn toàn khác nhau**:

| Khả năng | Ai phải làm gì |
|---|---|
| 1. BE đã làm đúng, chỉ là Swagger chưa mô tả | **BE không phải đụng code.** FE cập nhật `API_DOCS.md` và bỏ dấu `[VERIFY]` |
| 2. BE có làm nhưng khác cách FE đang đoán | **FE sửa,** BE giữ nguyên |
| 3. BE thật sự chưa có | Hai bên **cùng bàn** xem có cần làm không, hay để ngoài phạm vi đồ án |

Bọn mình dự đoán **phần lớn sẽ rơi vào khả năng số 1** — tức là BE không phải sửa gì cả, chỉ cần
xác nhận bằng lời là xong. Nhưng nếu FE tự đoán mà đoán sai thì sẽ ra đúng loại bug đã xảy ra ba
lần rồi: app không báo lỗi, không crash, chỉ là hiển thị sai hoặc bấm vào không đi đâu — và thường
đến lúc demo mới lộ.

**Mục tiêu cuối cùng:** có một bức tranh thống nhất giữa BE và FE cho toàn hệ thống, để `API_DOCS.md`
bên FE và source bên BE nói cùng một thứ. Sau khi có câu trả lời, hai bên mới ngồi lại quyết định
cái nào cần đổi thật.

**Cách trả lời mong muốn:** mỗi câu chỉ cần **kết luận + tên file/hàm/enum trong source BE** để bọn
mình đối chiếu. Không cần viết code hộ, không cần giải thích dài. Trả lời "đúng rồi, ở file X" là đủ.

Câu nào gắn nhãn *(ưu tiên)* là câu ảnh hưởng tới thứ sẽ chạy lúc demo.

---

## Phần A — Thông báo và deep-link *(ưu tiên cao nhất)*

### Bối cảnh

Đây là phần bọn mình lo nhất, vì nó hỏng **im lặng**.

Khi người dùng bấm vào một thông báo, FE quyết định mở màn hình nào dựa trên field `referenceType`
trong payload — xem `lib/navigation/notification_router.dart`. FE đang xử lý 10 giá trị:

```
SOS_ALERT, ALBUM_MEDIA, JOIN_REQUEST, FAMILY, FAMILY_MEMBER,
TASK_ASSIGNMENT, CALENDAR_EVENT, BUDGET_ALERT, FINANCIAL_GOAL, CONVERSATION
```

Nhưng trong bản Swagger 09/08:

- Không có schema nào tên chứa `Notification` (0 kết quả trên 253 schema)
- `GET /api/v1/families/{familyId}/notifications` **không có response schema** cho mã 200
- Chuỗi `referenceType` và `SOS_ALERT` **không xuất hiện một lần nào** trong toàn bộ file Swagger

Tức là 10 giá trị trên là FE **tự đọc từ response thật** rồi hardcode. Nếu BE đổi tên một giá trị,
hoặc thêm loại thông báo mới, thì bấm vào thông báo đó **không mở gì cả và cũng không báo lỗi** —
người dùng chỉ thấy nó nằm im trong danh sách.

### A1. `referenceType` được khai ở đâu trong source BE, và tập giá trị đầy đủ là gì? *(ưu tiên)*

Là Prisma enum, TypeScript union type, hay chỉ là `string` tự do?

**Cần biết:** tên file + danh sách đầy đủ giá trị.

### A2. Một object notification trả về có đúng những field nào? *(ưu tiên)*

FE hiện đọc: `id`, `title`, `body`/`message`, `referenceType`, `referenceId`, `isRead`, `createdAt`.

**Cần biết:** danh sách field thật (tên DTO trong BE là đủ), và có field nào FE đang bỏ sót không.

### A3. Với mỗi `referenceType`, `referenceId` trỏ tới id của bảng nào?

Ví dụ `FINANCIAL_GOAL` → id của goal, `FAMILY_MEMBER` → `userId` hay `memberId`?

Chỗ này quan trọng vì FE dùng `referenceId` để mở thẳng màn hình chi tiết
(`/manager/goal-detail?goalId=...`, `/manager/member/{id}`). Nếu FE hiểu nhầm `memberId` thành
`userId` thì màn hình chi tiết mở ra rỗng.

### A4. Payload FCM (lúc app đang ở nền / đã tắt) có giữ nguyên `referenceType` và `referenceId` không? *(ưu tiên)*

Lúc app ở nền, FE **chỉ có** payload FCM, không gọi được API. Nếu payload FCM dùng tên field khác
với REST (ví dụ `type` thay vì `referenceType`) thì deep-link từ thông báo đẩy sẽ chết, trong khi
deep-link từ danh sách trong app vẫn chạy — rất khó phát hiện khi test.

**Cần biết:** cấu trúc phần `data` của message FCM, và tên hàm build payload đó trong BE.

### A5. Có loại thông báo nào BE đang gửi mà chưa nằm trong 10 giá trị FE xử lý không?

Hiện FE gặp giá trị lạ thì để nguyên trong danh sách, không mở gì (fail-open, không crash). Bọn
mình muốn biết có đang bỏ sót loại nào đáng lẽ phải mở được màn hình.

### A6. `POST /finance/support-requests` có phát thông báo cho Manager/Deputy không? Và khi duyệt/từ chối có báo lại người gửi không?

Câu này treo từ 28/07, chưa có câu trả lời.

---

## Phần B — Đăng nhập Google khi email đã tồn tại *(ưu tiên)*

### Bối cảnh

`POST /api/v1/auth/firebase` trong Swagger khai 4 mã trả về (200 / 401 / 403 / 503) nhưng
**không mã nào có response schema**. FE không biết body trả về có gì.

Tình huống cần xử lý: người dùng đã đăng ký bằng email + mật khẩu, sau đó bấm "Đăng nhập bằng
Google" với **cùng email đó**.

### B1. BE đang xử lý tình huống đó thế nào? *(ưu tiên)*

Ba khả năng, cần biết là cái nào:

1. Tự động liên kết Google vào tài khoản email sẵn có, trả token bình thường
2. Tạo một tài khoản mới hoàn toàn (thành 2 tài khoản cùng email)
3. Trả lỗi 409 / 400 bắt người dùng đăng nhập bằng mật khẩu

**Cần biết:** khả năng nào, và tên hàm xử lý trong BE.

### B2. Response body của 200 có đúng những field nào?

Có `accessToken` / `refreshToken` / `user` như `/auth/login` không, hay khác?

### B3. BE có lưu lại "tài khoản này đăng nhập được bằng những cách nào" không?

Ví dụ một bảng `auth_providers`, hay một field kiểu `provider` trên `User`.

**Vì sao FE hỏi:** để màn hình hồ sơ hiển thị đúng "Đã liên kết Google" và để biết có được phép
đổi mật khẩu hay không. Nếu BE không lưu thì FE **không** làm màn hình đó, chứ không tự đoán.

### B4. 503 "Google login not configured" xảy ra khi nào?

Thiếu biến môi trường Firebase trên server? Bọn mình cần biết để phân biệt "server chưa cấu hình"
với "token của người dùng hỏng" — hai cái này phải hiện hai câu thông báo khác nhau.

---

## Phần C — Đồng hồ claim token *(ưu tiên)*

### Bối cảnh

Luồng ghép nối bằng mã kích hoạt đã chạy được hai chiều, cảm ơn Nghĩa. Còn đúng một chỗ chưa rõ.

`POST /api/v1/wearable-activations/{sessionId}/claim` trong Swagger **chỉ khai mã 200**, không có
mã lỗi nào. Trong khi endpoint sensor event ngay bên cạnh
(`POST /families/{familyId}/wearables/{deviceId}/events`) thì khai rất rõ:
`WEARABLE_NOT_PAIRED`, `INVALID_SENSOR_EVENT_TYPE`, `WEARABLE_ALREADY_PAIRED`,
`DEVICE_IDENTIFIER_TAKEN`.

### C1. `claim` trả về gì trong 4 tình huống sau? *(ưu tiên)*

| Tình huống | Mã HTTP? | `code`/`errorCode`? |
|---|---|---|
| Phiên đã hết hạn | ? | ? |
| Phiên đã claim rồi (gọi lần 2) | ? | ? |
| Điện thoại chưa nhập mã (status vẫn `PENDING`) | ? | ? |
| `sessionId` không tồn tại / sai | ? | ? |

**Vì sao FE cần:** màn hình đồng hồ rất nhỏ, chỉ hiện được một câu. Hiện tại mọi lỗi đều ra cùng
một câu chung chung, người dùng không biết nên bấm thử lại hay phải tạo mã mới.

### C2. Response lỗi có set `code`/`errorCode` không, hay chỉ có `message`?

FE thích bắt theo `code` hơn vì bắt theo `message` sẽ vỡ khi BE sửa câu chữ tiếng Việt.

### C3. Activation session sống bao lâu?

Có job dọn session hết hạn không, hay chỉ so `expiresAt` lúc gọi?

**Ghi chú:** FE đã cố tình **không** tự tính hết hạn bằng đồng hồ của thiết bị, mà chỉ tin
`status == 'EXPIRED'` do BE trả về. Lý do: máy ảo Wear OS lệch giờ rất nhiều, nếu tin đồng hồ máy
thì việc ghép nối sẽ bị khoá vĩnh viễn. Chỉ muốn xác nhận là cách hiểu này khớp với BE.

### C4. Token cấp cho đồng hồ có khác token thường không?

Cùng TTL 15 phút? Cùng quyền? Hay có scope riêng cho thiết bị?

### C5. Xác nhận lại câu A3 của bản 07/08: `POST /auth/logout` **có** truyền `refreshToken` thì chỉ thu hồi phiên đó, đúng không?

FE đã kiểm tra `auth_provider.dart` và xác nhận điện thoại **có** gửi `refreshToken` khi đăng xuất,
nên về lý thuyết phiên của đồng hồ vẫn sống. Chỉ cần Nghĩa xác nhận là BE thật sự làm vậy, vì kịch
bản "đăng xuất trên điện thoại → đồng hồ chết im" mà xảy ra lúc demo thì rất khó chữa tại chỗ.

---

## Phần D — Tài chính theo hũ (`byJar`)

### Bối cảnh

`FinanceSpendingSummaryResponseDto.byJar` trong Swagger đang là:

```json
"byJar": { "example": [], "description": "Reserved", "type": "array", "items": { "type": "object" } }
```

Vì `byJar` luôn rỗng nên FE đang **tự tính thực chi từng hũ ở phía client**
(`lib/utils/jar_allocation.dart`), bằng cách gộp các ledger entry theo category rồi map sang hũ.

### D1. Trong DB có quan hệ trực tiếp giữa một ledger entry và một hũ không?

Ví dụ `LedgerEntry.jarId`, hay phải suy ra qua `category → jar`?

**Vì sao FE hỏi:** nếu BE có quan hệ trực tiếp thì con số BE tính được sẽ **khác** con số FE đang
tự tính, và con số của BE mới là đúng. Với môn tài chính thì đây là chỗ dễ bị hội đồng hỏi nhất.

### D2. `byJar` là đang định làm, hay là quyết định bỏ?

Câu này thuần thông tin. Nếu BE trả lời "không làm nữa" thì FE ghi rõ trong tài liệu là **số liệu
theo hũ được tính ở client** để không ai hiểu nhầm đó là số của server — chứ FE không đề nghị BE
phải làm.

### D3. Có job nào tự động chốt kỳ tài chính hàng tháng không?

`wallet_screen.dart` hiện giả định là không có. Chỉ cần xác nhận.

---

## Phần E — Trợ lý AI

### Bối cảnh

`AiActionType` đã được khai đủ 9 giá trị trong Swagger — cảm ơn, FE đã ghim 9 giá trị này bằng
một contract test tự động, nên nếu BE thêm hoặc đổi tên thì suite của FE sẽ đỏ ngay thay vì hỏng
im lặng. Phần này ổn rồi.

Còn lại một chỗ: `AiConfirmActionDataResponseDto.result` trỏ tới `AiActionResultResponseDto`, mà
schema đó chỉ có **đúng một field `id`**.

### E1. Sau khi người dùng xác nhận một action `CREATE_LEDGER_ENTRY`, bản ghi được tạo có `categoryId` không?

**Vì sao FE hỏi:** nếu không có category thì giao dịch do AI tạo sẽ rơi vào nhóm "chưa phân loại",
người dùng phải tự vào sửa lại — làm mất gần hết ý nghĩa của việc nhờ AI ghi hộ.

### E2. AI chọn category bằng cách nào?

Tự suy từ nội dung câu người dùng nhập, hay lấy một category mặc định, hay để trống?

### E3. `result` chỉ trả `id` là cố ý hay tạm thời?

FE hiện gọi lại API để lấy chi tiết bản ghi vừa tạo. Cách này chạy được, chỉ là tốn thêm một
request. Không đề nghị đổi — chỉ hỏi để ghi đúng vào tài liệu.

---

## Phần F — Quan hệ gia đình và hồ sơ cá nhân

### Bối cảnh

Enum `relationship` (`FATHER | MOTHER | SPOUSE | CHILD | SISTER | BROTHER | GRANDPARENT | OTHER`)
chỉ xuất hiện ở đúng 3 chỗ trong Swagger:

- `CreateFamilyDto` — lúc tạo gia đình
- `ApproveJoinRequestDto` — lúc Manager duyệt người vào
- `AdminUpdateMemberDto` — **chỉ SYSTEM_ADMIN gọi được**

`UpdateMyProfileDto` thì chỉ có `fullName`, `phone`, `avatarUrl`.

### F1. Có route nào cho phép người trong gia đình sửa `relationship` không, kể cả chưa expose ra Swagger? *(ưu tiên)*

**Vì sao hỏi:** nếu thật sự không có, thì Manager chọn nhầm quan hệ lúc duyệt (chọn "Con" thay vì
"Vợ/Chồng") là **kẹt vĩnh viễn** — không ai trong gia đình sửa được, phải nhờ SYSTEM_ADMIN vào DB.
Đây là thứ rất dễ xảy ra lúc demo vì lúc duyệt người ta bấm nhanh.

Bọn mình **chưa đề nghị làm endpoint mới**. Muốn biết chắc là "không có" trước, rồi mới bàn xem
đây có phải vấn đề đáng sửa trong phạm vi đồ án hay không.

### F2. `avatarUrl` trong `PATCH /auth/me` nhận URL từ đâu?

Swagger có `avatarUrl` nhưng bọn mình chỉ tìm thấy 2 endpoint upload file, cả hai đều không phải
cho avatar:

- `POST /families/{familyId}/tasks/proofs/upload`
- `POST /families/{familyId}/chat/conversations/{conversationId}/messages/upload`

**Cần biết:** có endpoint upload avatar không, hay là dùng chung một trong hai cái trên, hay là
người dùng phải tự dán URL ngoài vào? Hiện FE **không** hiện ô sửa avatar vì không biết luồng đúng.

### F3. Sửa `relationship` có ràng buộc nghiệp vụ nào không?

Ví dụ một gia đình chỉ được có một `FATHER`? Hỏi để nếu sau này có làm thì FE validate đúng ngay
từ đầu.

---

## Phần G — Yêu cầu hỗ trợ chi tiêu

### Bối cảnh

Cả 5 endpoint `support-requests` đều **không có response schema**. Đây chính là chỗ đã gây bug tên
người gửi luôn hiện "Thành viên" (đã sửa ngày 29/07), và cách sửa hiện tại là FE **đoán** khuôn
object lồng dựa theo các DTO member khác.

### G1. Một object support request trả về có đúng những field nào? *(ưu tiên)*

Cụ thể là thông tin người gửi nằm ở đâu:

- `requesterMember.user.fullName`? (FE đang đoán thế này)
- `requester.fullName`?
- hay chỉ có `requesterMemberId` và FE phải tự tra tên?

**Cần biết:** tên DTO trong BE là đủ, bọn mình tự đọc.

---

## Phần H — Về việc thiếu response schema nói chung

### Bối cảnh

Bọn mình đếm được **148 trên 296 operation (50%) không có response schema JSON cho mã 2xx**, trong
đó có gần như toàn bộ module auth (`register`, `login`, `refresh`, `me`, `verify-email`,
`forgot-password`, `reset-password`) và cả `GET /wearables/me`.

Đây **không phải lời phàn nàn** — API vẫn chạy đúng, FE vẫn gọi được. Nhưng nó có nghĩa là mọi tên
field FE đọc từ nửa số API đều là quan sát từ response thật chứ không có gì bảo chứng. Ba bug tốn
thời gian nhất từ đầu dự án đều sinh ra từ đúng chỗ này: `locationPoints` của bản đồ SOS, tên người
gửi support request, và DTO reward settlement.

### H1. Nguyên nhân là gì?

Bọn mình đoán là do BE dùng interceptor bọc envelope `{ success, data }` nên `@ApiResponse` khai
`type` sẽ ra sai lồng nhau, và cách nhanh nhất là bỏ trống. Có đúng vậy không?

### H2. Có cách nào rẻ để bổ sung không?

Ví dụ một generic wrapper kiểu `ApiResponseDto<T>` khai một lần rồi dùng lại, thay vì viết DTO cho
từng endpoint.

**Nói rõ:** bọn mình **không** đề nghị BE đi khai lại 148 chỗ. Chỉ muốn biết nếu sau này có làm thì
tốn cỡ nào — để hai bên cùng quyết định có đáng làm trong phạm vi đồ án không, hay cứ để FE ghi
bằng tay vào `API_DOCS.md`.

---

## Sau khi có câu trả lời thì FE làm gì

Để Nhật và Nghĩa yên tâm là trả lời xong sẽ không phát sinh một đống việc:

1. **Câu nào BE trả lời "đúng rồi, ở file X"** → FE cập nhật `API_DOCS.md`, bỏ dấu `[VERIFY]`.
   **BE không phải đụng gì.** Bọn mình dự đoán phần lớn rơi vào nhóm này.
2. **Câu nào lộ ra FE đang hiểu sai** → FE tự sửa code bên mình, có test mapping kèm theo.
   **BE cũng không phải đụng gì.**
3. **Câu nào lộ ra thứ BE thật sự chưa có** → bọn mình mới viết một file `DE_XUAT_BE_*.md` riêng,
   ghi rõ **Bắt buộc** hay **Nên có**, để cả nhóm cùng quyết định. Và kể cả lúc đó cũng là đề xuất
   để bàn, không phải yêu cầu.

Những mục ưu tiên nếu không đủ thời gian trả lời hết: **A1, A2, A4** (thông báo), **B1** (Google
login), **C1** (lỗi claim), **F1** (sửa quan hệ), **G1** (support request). Năm chỗ này là thứ sẽ
lộ ra lúc demo.

---

## Phụ lục — những chỗ FE đã kiểm tra và xác nhận KHÔNG có vấn đề

Ghi ra để khỏi mất thời gian của Nghĩa rà lại:

| Nội dung | Kết luận |
|---|---|
| `CreateSosAlertDto.sourceType` | Khớp. FE gửi `WEARABLE` từ đồng hồ, `MOBILE_APP` từ điện thoại |
| `PushSosLocationDto.sourceType` | Khớp. FE gửi `WEARABLE_GPS` / `MOBILE_GPS`, có kèm `deviceId` |
| `SosAlertResponseDto.locationPoints` | Khớp. FE đã sửa để đọc đúng field này (bug bản đồ SOS đã hết) |
| `AiActionType` | Khớp đủ 9 giá trị, đã có contract test tự động ghim lại |
| `PairWearableDto`, `CreateWearableActivationDto`, `UpdateWearableDto` | Khớp hoàn toàn |
| `SensorEventResponseDto.eventType` | Khớp. FE gửi `FALL_DETECTED` và `HEART_RATE_ABNORMAL` |
| `DELETE /families/{id}/wearables/{deviceId}` | Đã có, đúng như đề xuất 06/08. Cảm ơn |
| Mã lỗi của endpoint sensor event | Đã khai rõ trong Swagger, FE bắt được. Cảm ơn |

Toàn bộ số liệu trong tài liệu này lấy từ bản Swagger 09/08/2026 và nhánh FE `giap` tại commit
`bd09896` (39 file test / 399 test case, `flutter analyze --no-fatal-infos` 0 error 0 warning).
