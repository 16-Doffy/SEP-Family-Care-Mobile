# Câu hỏi cho BE — SOS push/socket, ngày 2026-08-11

**Người hỏi:** FE Mobile (Flutter — app điện thoại + app Wear OS dùng chung codebase)
**Người nhận:** Nhật (lead) / Nghĩa, hoặc AI đang đọc source BE
**Bản Swagger đang dùng:** **236 path / 303 operation / 262 schema**, tải trực tiếp từ
`https://api.familycare-digital.com/api/docs-json` lúc soạn file này (11/08/2026).

> 📞 **Câu hỏi về Video Call đã tách sang file riêng: `CAU_HOI_BE_VIDEO_CALL_2026-08-11.md`**
> (dài hơn, gửi cho VQuanCT). File này chỉ còn đúng **một** câu hỏi về SOS, để người trả lời khỏi
> phải đọc lẫn hai chủ đề không liên quan.

---

## ⚠️ Đọc trước

**Đây CHỈ LÀ CÂU HỎI**, không yêu cầu BE đổi code/schema/deadline gì. Nhiều khả năng BE đã làm đúng
rồi và chỉ cần xác nhận bằng lời — FE **không phải sửa gì** nếu câu trả lời đúng như đang giả định.

---

## SOS: BE có bắn push/socket khi có alert mới không *(câu hỏi cũ, treo từ 04/08)*

Câu hỏi này đã ghi ở `API_DOCS.md` mục "SOS — `autoCreateAlertFromFall`" (dòng cuối) từ
2026-08-04 nhưng chưa có câu trả lời.

**Hỏi:** Khi có `POST /families/{familyId}/sos/alerts` (thủ công) hoặc SOS tự tạo qua
`POST .../wearables/{deviceId}/events`, BE có chủ động bắn thông báo (push FCM và/hoặc
`notification:new` qua Socket.IO `/notifications`) cho các thành viên khác không? Nếu có, bắn cho
**ai** — toàn bộ thành viên active, hay chỉ Manager/Deputy theo cấu hình
`SosSettings.notifyAllMembers`?

FE hiện đã có đủ 3 kênh nhận (FCM `POST /devices/tokens`, Socket.IO `/notifications`, REST poll
fallback trong `SosProvider.fetchAlerts()`) — chỉ cần xác nhận phía gửi có hoạt động đúng phạm vi
mong muốn hay không, **không cần BE code thêm gì** nếu câu trả lời là "có, đúng rule trên".

**Vì sao quan trọng:** nếu BE không bắn mà chỉ dựa vào REST poll của FE thì cảnh báo SOS tới chậm
theo chu kỳ poll, và **hoàn toàn không tới** khi app đang ở nền — đúng lúc cần nhất.
