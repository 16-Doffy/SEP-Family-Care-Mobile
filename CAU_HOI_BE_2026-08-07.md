# Câu hỏi cho BE — Mobile/Wear OS, ngày 2026-08-07

**Người hỏi:** FE Mobile (Flutter — app điện thoại + app Wear OS dùng chung codebase)
**Cách trả lời:** mỗi câu chỉ cần **kết luận + tên file/hàm trong BE** để bọn mình đối chiếu. Không cần viết code hộ.

Bản Swagger bọn mình đang dùng: **223 path / 290 operation / 245 schema**, lấy ngày 07/08/2026.
Những câu dưới đây đều là thứ **Swagger không trả lời được** — phải đọc source BE mới biết.

---

## Phần A — Đồng hồ lấy quyền gọi API bằng cách nào (CHẶN)

### Bối cảnh

Luồng ghép nối đã chốt và FE đã làm xong:

1. App trên đồng hồ hiện mã `FCW-XXXXXX`
2. Người dùng gõ mã đó trên điện thoại
3. Điện thoại gọi `POST /api/v1/families/{familyId}/wearables` với `deviceIdentifier = FCW-XXXXXX`

Nhưng việc này chỉ **tạo bản ghi thiết bị trong DB**. Nó không cấp gì cho **tiến trình app đang chạy trên đồng hồ**.

Mà app trên đồng hồ phải tự gọi được 2 endpoint sau, tức là **phải có access token riêng**:

- `GET /api/v1/wearables/me` — để biết `deviceId` của chính nó
- `POST /api/v1/families/{familyId}/wearables/{deviceId}/events` — để bắn sự kiện cảm biến (té ngã, nhịp tim)

Hiện tại FE đang phải bắt người dùng **gõ email + mật khẩu trên màn hình đồng hồ**. Bàn phím đồng hồ tròn, gõ mật khẩu có ký tự đặc biệt rất khó.

### A1. Có cơ chế nào trong BE cho phép thiết bị đổi mã/định danh lấy token không? *(chặn)*

Bọn mình đã rà cả 290 operation, không thấy. Nhưng có thể BE có guard / strategy / module nội bộ chưa expose ra Swagger.

**Cần biết:** Có / Không. Nếu có thì ở đâu, route nào.

### A2. Nếu không có — nhóm chấp nhận đăng nhập tay trên đồng hồ cho bản demo chứ? *(chặn)*

Đây là câu hỏi quyết định, không phải câu hỏi kỹ thuật. Trả lời "được, cứ đăng nhập tay" là bọn mình đóng việc này lại luôn và bỏ chữ "tạm" trên nút đăng nhập.

### A3. `POST /auth/logout` không truyền `refreshToken` thì revoke **toàn bộ** session của user — đúng không? *(chặn)*

Swagger ghi: *"Pass refreshToken to revoke only this device, omit to revoke all"*.

**Vì sao FE cần biết:** nếu đúng, thì kịch bản này sẽ xảy ra ngay khi demo:

> Người dùng đăng nhập trên đồng hồ → đăng nhập trên điện thoại → **thoát tài khoản trên điện thoại** → đồng hồ mất session, SOS cảm biến trên đồng hồ chết im, không có thông báo gì.

**Cần biết:**
- Xác nhận hành vi revoke-all
- Refresh token có **giới hạn số phiên đồng thời** trên một tài khoản không? (đồng hồ + điện thoại = 2 phiên)
- Refresh token sống bao lâu? Nếu đồng hồ để yên 3 ngày rồi mở lại, có phải đăng nhập lại không?

---

## Phần B — Định danh thiết bị (`deviceIdentifier`)

### Bối cảnh

Mã `FCW-XXXXXX` do **app trên đồng hồ tự sinh** và lưu trong secure storage của đồng hồ. Cài lại app / reset máy ảo là ra mã mới.

### B1. `deviceIdentifier` unique trong phạm vi nào?

Swagger ghi *"unique within a family"*. Nhưng lỗi `DEVICE_IDENTIFIER_TAKEN` được mô tả là "đã pair bởi user khác".

**Cần biết:** unique index đặt trên `(familyId, deviceIdentifier)` hay chỉ `deviceIdentifier`? Tức là hai gia đình khác nhau có được dùng trùng mã không?

### B2. Bản ghi `UNPAIRED` có còn giữ chỗ `deviceIdentifier` không?

**Vì sao FE cần biết:** nhóm test dùng chung máy ảo Wear OS. Nếu tài khoản A ghép mã `FCW-ABC123` rồi gỡ (`UNPAIRED`), tài khoản B trong **cùng gia đình** nhập lại đúng mã đó thì:

- (a) Pair lại được bình thường
- (b) `409 DEVICE_IDENTIFIER_TAKEN` vì bản ghi cũ còn nằm đó

**Cần biết:** (a) hay (b). Nếu là (b) thì FE phải hướng dẫn người dùng **xóa hẳn** thiết bị chứ không chỉ gỡ.

### B3. Đang PAIRED với mã cũ, muốn đổi sang mã mới thì làm sao?

**Bối cảnh:** trước đây FE gửi định danh dạng `familycare-xxxxxxxxxx` (sinh trên điện thoại, sai luồng). Nay đổi sang mã `FCW-XXXXXX` lấy từ đồng hồ. Tất cả tài khoản test của nhóm đang có bản ghi PAIRED với định danh kiểu cũ.

Bọn mình hiểu là phải: gỡ (`PATCH pairingStatus: UNPAIRED`) → rồi ghép lại bằng mã mới.

**Cần biết:** đúng không, hay còn cách nào gọn hơn (ví dụ `PATCH` đổi thẳng `deviceIdentifier`)? Swagger không cho `deviceIdentifier` vào `UpdateWearableDto`, nên bọn mình đoán là không.

### B4. BE có ràng buộc format cho `deviceIdentifier` không?

**Vì sao FE cần biết:** người dùng gõ tay, gõ sai là tạo ra một bản ghi PAIRED không ứng với đồng hồ nào. FE muốn validate ngay tại ô nhập.

**Cần biết:** BE có validate format không (regex, độ dài)? Hay nhận mọi chuỗi? Nếu BE không validate, FE sẽ tự chặn theo đúng format `FCW-` + 6 ký tự.

---

## Phần C — Sự kiện cảm biến

### C1. `HEART_RATE_ABNORMAL` có được BE kiểm tra ngưỡng không, hay cứ nhận event là tạo SOS?

**Bối cảnh:** BE đã chốt `HEART_RATE_ABNORMAL` **auto tạo SOS**. `rawValue` bọn mình gửi:

```json
{ "heartRate": 38, "thresholdLow": 50, "durationSeconds": 30, "source": "wear_os_emulator" }
```

**Cần biết:** BE có đọc `heartRate` để so với `thresholdLow`/`thresholdHigh` rồi mới quyết định tạo SOS không? Hay chỉ cần `eventType = HEART_RATE_ABNORMAL` là tạo, `rawValue` chỉ để lưu?

**Vì sao quan trọng:** nếu BE không kiểm tra, thì mọi ngưỡng phải do **đồng hồ** tự quyết. Đồng hồ gửi nhầm là cả nhà bị báo động thật.

### C2. Không gửi `severity` thì BE tự tính hay để `null`?

`CreateSensorEventDto.severity` là optional. FE **cố ý không gửi** cho `HEART_RATE_ABNORMAL` (spec Wear OS chỉ ghi `severity` cho té ngã).

**Cần biết:** BE tự suy ra `severity` từ `rawValue`, hay lưu `null` và SOS tạo ra cũng không có mức độ? Nếu là `null` thì màn hình của người thân sẽ hiện "Không phân loại" cho mọi SOS từ nhịp tim — có đúng ý không?

### C3. `alert.message` do BE sinh — nội dung có sẵn cho từng loại event chứ?

BE đã xác nhận `alert.message` cho SOS từ wearable là BE sinh, mobile chỉ hiển thị.

**Cần biết:** cho xin **nguyên văn** câu message của từng `eventType` (`SOS_BUTTON_PRESSED`, `FALL_DETECTED`, `HEART_RATE_ABNORMAL`) để FE kiểm tra hiển thị không bị tràn / cắt chữ trên màn hình nhỏ.

---

## Phần D — Đăng nhập Google (`POST /auth/firebase`)

### Bối cảnh

FE đã nối endpoint này. Swagger khai 4 mã lỗi: `200 / 401 / 403 / 503`. **Không có `409`.**

### D1. Email đã đăng ký bằng mật khẩu, giờ đăng nhập Google cùng email đó thì sao?

Ba khả năng, cần biết BE làm cái nào:

- (a) **Gộp** vào tài khoản cũ, trả token của tài khoản đó
- (b) **Tạo tài khoản mới** → người dùng mất sạch dữ liệu gia đình cũ mà không hiểu vì sao
- (c) **Từ chối** bằng một mã lỗi nào đó

**Vì sao quan trọng:** nếu là (b) thì đây là lỗi nghiêm trọng và FE không có cách nào phát hiện — cả hai trường hợp đều trả `200` kèm token hợp lệ.

### D2. Chiều ngược lại: đăng ký bằng Google trước, sau đó dùng "Quên mật khẩu" cho email đó?

Tài khoản Google không có mật khẩu. `POST /auth/forgot-password` luôn trả `200` (chống dò email), nên FE không biết được.

**Cần biết:** BE có gửi OTP cho tài khoản Google không? Đặt lại mật khẩu xong thì tài khoản đó đăng nhập được bằng **cả hai** cách chứ?

### D3. Tài khoản đăng nhập bằng Google có `verificationStatus = VERIFIED` luôn không?

**Vì sao FE cần biết:** router của app chặn người dùng chưa xác thực email ở màn nhập OTP. Nếu tài khoản Google trả về `UNVERIFIED`, người dùng sẽ bị kẹt ở màn hình chờ OTP mà email Google thì chẳng bao giờ nhận được mã.

---

## Tóm tắt mức độ ưu tiên

| Mã | Nội dung | Mức độ |
|---|---|---|
| A1, A2 | Đồng hồ lấy token bằng cách nào / chấp nhận đăng nhập tay? | **Chặn** |
| A3 | Logout trên điện thoại có giết session đồng hồ không | **Chặn** |
| D1 | Đăng nhập Google trùng email — gộp hay tạo mới | **Chặn** |
| B2, B3 | UNPAIRED có giữ chỗ mã / cách đổi mã cũ sang mã mới | Cao |
| C1 | BE có kiểm tra ngưỡng nhịp tim không | Cao |
| B1, B4, C2, C3, D2, D3 | Còn lại | Nên biết |
